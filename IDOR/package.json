{
  "scripts": {
    "install-all": "npm --prefix client install && npm --prefix server install",

    "build": "npm --prefix client run build && rm -rf server/static && mkdir -p server/static && cp -r client/dist/* server/static",

    "start": "node server/server.js"
  }
}
